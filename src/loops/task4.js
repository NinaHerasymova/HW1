"use strict"

function factorial(num)
            {
                var val = 1;
                
                for (var i = 2; i <= num; i++)
                    val = val * i;
                    console.log(val);
                } 
            
            
           factorial(4)